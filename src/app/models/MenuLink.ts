export class MenuLink{
    constructor(title:string, route:string, icon:string){
        this.title = title;
        this.route = route;
        this.icon = icon;
    }
    title:string;
    route:string;
    icon:string;
}